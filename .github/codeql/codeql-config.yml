name: "Edgaze CodeQL Config"

queries:
  - uses: security-extended
  - uses: security-and-quality

paths:
  - src
  - scripts

paths-ignore:
  - node_modules
  - .next
  - dist
  - build
  - coverage
  - public
  - var
